{
  "name": "mcp-live-demo",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "repository": { "type": "git", "url": "git+https://github.com/freeappstore-online/mcp-live-demo.git" },
  "scripts": {
    "dev": "pnpm --filter @mcp-live-demo/web dev",
    "build": "pnpm --filter @mcp-live-demo/web build",
    "preview": "pnpm --filter @mcp-live-demo/web preview",
    "typecheck": "pnpm --filter @mcp-live-demo/web exec tsc -b",
    "test": "pnpm --filter @mcp-live-demo/web exec tsc -b"
  }
}
